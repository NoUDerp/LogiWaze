{
  "name": "parcel-namer-stable-fonts",
  "version": "1.0.0",
  "description": "Parcel namer that emits font assets (woff2/ttf) with their source filename — no content-hash. Falls through to the default namer for everything else.",
  "main": "index.js",
  "private": true,
  "engines": {
    "parcel": "^2.0.0"
  }
}
